'use client'

export default function TestAuth() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-4">Auth System Test</h1>
      <p>This page tests if we can load without auth errors.</p>
      <p className="mt-4 text-green-600">If you see this, the page loaded successfully!</p>
    </div>
  )
}